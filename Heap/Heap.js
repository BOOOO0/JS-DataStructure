// heap 자료구조는 항상 정렬이 되어 있어야 한다

export default class Heap {
  constructor() {
    this.heapContainer = [];
  }
  // 다른 메소드를 구현하기에 앞서
  // downHeap과 upHeap으로 정렬해주는 메소드의 구현이 우선이다
  heapifyDown() {}
  heapifyUp() {}
}
